int main() {
  bool b = false; // 0
  bool b2 = true; // 1 // anything not zero is true
  bool b3 = 3 < 4; // 1
  bool b4 = 5; // true

  if (b4) {

  }
  else {

  }

  double sum = 0;
  for (int i = 0; i < 10; i++) // .1 + .1 + .1 + ...
    sum += 0.1; //~1.0
  
  bool b5 = (sum == 1); // false 

  double a = .25 + .5;
  if (a == 0.75) { //YES but don't do this

  }

}